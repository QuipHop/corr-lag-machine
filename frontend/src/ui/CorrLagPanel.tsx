export default function CorrLagPanel() {
  return (
    <div style={{ padding: 12 }}>
      <h3>Lag Analysis (coming soon)</h3>
      <p>v1 keeps <b>lag = 0</b> only (monthly-only; no cross-frequency, no lag scan).</p>
      <p>We’ll add optional lags and cross-correlation in v2 once monthly flow is solid.</p>
    </div>
  );
}
